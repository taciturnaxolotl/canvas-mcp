# Server Configuration
PORT=3000
HOST=localhost
BASE_URL=http://localhost:3000

# Database
DATABASE_PATH=./canvas-mcp.db

# Encryption (generate with: bun run generate-key)
ENCRYPTION_KEY=

# SMTP Configuration (Mailchannels)
SMTP_HOST=smtp.mailchannels.net
SMTP_PORT=587
SMTP_USER=kieranklukascontracting
SMTP_PASS=
SMTP_FROM=canvas-mcp@dunkirk.sh

# DKIM Configuration (optional but recommended)
DKIM_SELECTOR=mailchannels
DKIM_DOMAIN=dunkirk.sh
DKIM_PRIVATE_KEY_FILE=./dkim_private.pem

# JWT Keys for OAuth (generate with openssl)
# openssl genrsa -out private.pem 2048
# openssl rsa -in private.pem -pubout -out public.pem
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
